import { FormattingOptionsPipe } from './formatting-options.pipe';

describe('FormattingOptionsPipe', () => {
  it('create an instance', () => {
    const pipe = new FormattingOptionsPipe();
    expect(pipe).toBeTruthy();
  });
});
